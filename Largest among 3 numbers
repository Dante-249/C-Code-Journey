using namespace std;

void find_max(int ,int , int);

void find_max(int a,int b,int c)
{

int max;
max=a;
if(b>max)
max=b;
if(c>max)
max=c;
cout<<max<<endl;
return void();
}

int main()
{
    int a,b,c;
    cin>>a>>b>>c;
    find_max(a,b,c);
    return 0;
}
